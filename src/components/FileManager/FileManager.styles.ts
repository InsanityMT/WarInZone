import styled from 'styled-components'

import { Form } from 'react-bootstrap'
import { colors } from '../../constants/colors'

export const FileManagerContainer = styled(Form.Group)`
  width: 60%;
  color: ${colors.white};
`
